# 电脑端预览 — 验收记录

日期：2026-08-27。对象为正式 Taro H5 实现，不以 A/B/C HTML 初稿代替。浏览器证据位于 `design-demos/screenshots/implemented/`。

| 编号 | 实际验证与结果 | 状态 |
|---|---|---|
| DA-01 | ego-browser 在 1024×768、1280×720、1440×900 检查 B 三栏首页；品牌 PNG、角色场景、输入与导航均在视口内 | 通过 |
| DA-02 | 真实 UI 输入作品集场景，完成授权、任务生成、太难降阶、换入口、2 分钟计时（02:00→01:58）、完成反馈；未改浏览器状态跳步 | 通过 |
| DA-03 | 三种分享模板可切换；下载出院证明 PNG 并查看 1300×1800 成品；记录、任务详情、调整过程、空态均可访问 | 通过 |
| DA-04 | 所有 H5 页面显示「本地规则 · 未接入 AI」；Performance 资源检查无 cloud/model/OpenAI 等请求；输入以内存转交且 URL 无原话 | 通过 |
| DA-05 | 32 个正式状态共 79 个视口记录无应用内裁切/越界；320/360/390 的 `.web-preview-frame` 和滚动页宽等于视口，气泡与卡片无重叠；双端构建通过 | 通过（浏览器手机等效视口，不等同微信真机） |
| DA-06 | 首页及会话中途输入高风险文本均只显示安全回应；独立 10088 源选择「仅完成这一次」后 session/memory 仍为 0，分享草稿仅暂存 30 分钟 | 通过 |
| DA-07 | 授权、追问、任务、照顾、计时、反馈、完成、分享、记录、详情、发现、我的、安全、弹窗等状态均在三个桌面基准中无整页滚动；长内容、对话、图鉴和记录使用分页/详情 | 通过 |
| DA-08 | 三项 Skill 的来源、授权、安装提交与使用范围记录于 `skill-review.md`；正式实现复用 Taro，未运行外部云脚本 | 通过 |
| DA-09 | 修复 Taro H5 对原生标签选择器的转换问题；原生 dialog 支持 Escape 并恢复焦点；菜单方向键可移动焦点，Enter/Space 有显式路由处理 | 通过 |
| DA-10 | AI 生成收容室背景已正式接入；原始 PNG 和提示词保留，运行时另用 52 KB WebP；原品牌和固定 16 只怪兽未重画 | 通过 |
| DA-11 | 用户原话「我喜欢b方案」记录在 `direction-approved.md`；B 比例、色板、顶栏菜单在三个桌面视口均可见且无重叠 | 通过 |

## 自动检查

- `npm run typecheck`：通过。
- `npm run test:agent`：9/9 通过。
- `npm run test:client`：9/9 通过（新增一次性内存输入转交测试）。
- `npm run build:weapp`：通过；1,846,549 bytes，未包含 `src/desktop` 代码或场景大图。
- `npm run build:h5`：通过。首次构建提示场景 PNG 过大后，正式运行资源改为 768×1152 WebP；原 PNG 仅作设计源文件。

## 浏览器证据

- `layout.json`：32 个正式状态、79 个视口检查记录，最终 `outside=[]`、`clipped=[]`。
- `privacy.json`：无记忆完成后长期 session/memory 数量保持 0，真实计时 05:00→04:58。
- `clear-cancel.json`：打开清空二次确认后取消，前后本地数据计数一致，焦点返回触发按钮；未自动执行不可恢复删除。
- `keyboard.json`、`navigation-keyboard.json`：弹窗 Escape/焦点恢复、菜单键盘检查。导航最终实现对 Enter/Space 显式处理。
- `downloads/心情怪兽-怪兽出院证明.png`：由正式 H5 Canvas 下载的测试成品。

本轮没有接真实模型、没有部署公网、没有微信真机验收，也没有确认执行「清空本地数据」。H5 构建仍有 `app.js`/共享 chunk 的默认体积提示，属于后续公网性能优化项，不影响本地核心流程预览。


## 2026-09-23 推送前复验

本次针对待提交工作区重新执行以下检查：

- `git diff --check`：通过。
- `npm run typecheck`：通过。
- `npm run test:agent`：9/9 通过。
- `npm run test:client`：9/9 通过。
- `npm run build:weapp`：通过。
- `npm run build:h5`：通过；仍有 app.js（248 KiB）、共享 chunk（272 KiB）及入口总量（388 KiB）的体积提示。

本次仅复验上述自动检查与构建，未重新执行浏览器视觉、真实模型、云端部署或微信真机验收。上方 2026-08-27 的浏览器记录仍为历史证据。
