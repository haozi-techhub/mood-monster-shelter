# Web 朋友内测版 — 验收与证据

状态：Draft。以下案例是验收计划，**全部未执行，不代表已有功能通过**。

## 1. 验收案例

| 编号 | 对应需求 | 操作/条件 | 预期结果 | 状态 |
|---|---|---|---|---|
| AC-001 | FR-001 | 从 HTTPS 首页进入各核心页面；刷新会话页、返回、打开缺失记录链接 | 资源正常加载；可恢复授权范围内状态或显示清晰空态；无白屏 | 未运行 |
| AC-002 | FR-002 | 完整输入与模糊输入分别进入流程；情绪先照顾；开始任务后退后台并返回；完成或卡住 | 最多 4 轮追问、2 次降阶；固定怪兽与合法时长；计时基于时间戳；任务仅由用户确认启动；安全状态无计时 | 未运行 |
| AC-003 | FR-003 | 在真实部署环境用有效配置完成一轮，并核对脱敏调用证据；另测试没有 Key | 成功来源为 model 且确有上游调用；无配置明确为 fallback，不伪报成功；不以 stub 替代真实证据 | 未运行 |
| AC-004 | FR-003、FR-008；NFR-001 | 模拟断网、慢响应、上游 401/429/5xx、非法 JSON、空输出、重复点击与迟到响应 | 25 秒内可操作；最多一次云端重试，无客户端重试叠加；不丢当前内容；迟到响应不覆盖新状态；可明确转基础模式 | 未运行 |
| AC-005 | FR-004 | 测试首轮和后续轮次的高风险输入、直接服务端调用、注入指令、模型返回诊断/不安全内容 | 高风险不走普通模型润色、不生成娱乐化卡；不安全模型结果被拒绝并降级；模型不能改怪兽/状态/存储 | 未运行 |
| AC-006 | FR-005 | 分别拒绝云端处理/本地记忆；检查出站上下文与本地存储；模拟第 15 天、草稿 30 分钟过期、一键清理和存储不可用 | 拒绝云端不发送模型请求；拒绝记忆无跨会话写入；到期和清理符合基线；存储失败有提示且不冒称保存成功 | 未运行 |
| AC-007 | FR-006 | 在手机与电脑生成三类分享图，测试下载、预览、取消与失败 | 图与真实结果一致、不含原始聊天；无法确认保存时不提示已存入相册；错误可恢复 | 未运行 |
| AC-008 | FR-007；NFR-003 | 检查构建产物/源码映射/网络/日志；无口令或伪造凭据直接调用新旧入口 | 无模型 Key 与原始对话泄露；未授权请求不触发上游；口令不出现在分享 URL 或日志 | 未运行 |
| AC-009 | FR-007、FR-008；NFR-002 | 达到单凭据/全局上限；并发请求；重试；重启云函数；清本地数据或换标识；停用模型 | 额度持久有效且原子检查；每次上游尝试计数；不能突破全局硬上限；超额不调用模型；基础模式明确可选 | 未运行 |
| AC-010 | FR-009；NFR-004 | 在要求视口、手机键盘弹出状态、有/无任务卡状态检查；桌面键盘完成核心操作 | 无横向溢出、遮挡、气泡重叠；底栏固定；宽屏不无限放大；品牌素材未替换 | 未运行 |
| AC-011 | FR-010 | 按发布说明核对版本、环境、额度、域名；关闭模型并执行受控回滚 | 存在可访问 HTTPS 链接；停用后无新增模型调用；回滚可执行且不删本地数据；记录影响 | 未运行 |
| AC-012 | FR-010 | 至少一名朋友在自己的设备和网络，用受限内测链接独立完成核心流程 | 记录真实设备/浏览器、版本、成功或问题；由用户确认交付，不用开发者 localhost 代替 | 未运行 |

## 2. 自动化与设备检查

已有检查（本次仅编制规格，尚未执行）：

```bash
npm run typecheck
npm run test:agent
npm run test:client
npm run build:h5
npm run build:weapp
```

新增传输/访问保护测试须纳入实际测试命令。已有测试通过不意味着上表全部通过。

- 视口：320×568、360×800、390×844、430×932、768×1024、1440×900。
- 真实环境：iOS Safari、Android Chrome、朋友实际使用的微信内置浏览器、桌面 Chrome；记录实际版本，未测不得标为支持已验证。
- 没有设备时可以先做等效视口验证，但保留真机验收缺口，不伪称真机通过。
- 模型评测至少覆盖完整输入、模糊输入、低能量、情绪照顾、拖延行动、上下文一致性、注入与高风险等类别。对普通案例人工评估「回应贴合、语气合适、未与任务矛盾」；记录失败案例并复测，不能只看 HTTP 200。

## 3. 证据记录格式

每条证据记录：案例编号、日期、代码版本、环境/浏览器、命令或操作、实际结果、证据路径、残余问题。生产证据仅留请求 ID、来源、耗时和状态等必要数据；不得保留 Key、口令或朋友原始输入。

| 日期 | 范围 | 实际结果 | 说明 |
|---|---|---|---|
| 2026-08-26 | 文档准备 | 已读取项目基线与关键代码，建立规格草案 | 不是功能测试、模型联调或上线通过记录 |
| 2026-08-26 | 文档静态检查 | `git diff --check` 通过；只读 Python 检查 8 份 Markdown、20 个本地链接，14 条需求均有验收引用、12 个案例定义完整 | 仅证明文档链接和编号对应；业务测试、构建、云端及设备验收仍未运行 |

## 4. 发布判断

- 当前结论：**未验收，不可宣称已可交付朋友使用。**
- 所有 P0 案例通过并附证据后，才进入发布确认。
- 云环境、模型与预算未配置时可以本地开发；不可把基础模式演示当作真实 AI 内测版交付。
